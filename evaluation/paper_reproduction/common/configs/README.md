# Common Configuration

Keep only dimensions shared by all comparisons here: model revision, prefix
workload, layer count, block size, HBM limit, SSD image, and run phase. Paper
specific selector or scheduler parameters belong in that paper's directory.
