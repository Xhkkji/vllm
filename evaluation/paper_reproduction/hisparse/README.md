# HiSparse-Style Adapter

The adapter models sparse block selection plus layer-wise windows. It does not
copy SGLang runtime code. Residency and correction-read behavior are measured
through the common request API when an integration runner is added.
